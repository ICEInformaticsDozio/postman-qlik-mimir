$kind: http-request
description: Tabelle, campi, tipi e valori distinti dell'app — base per comporre l'espressione.
url: "{{baseUrl}}/apps/{{appId}}"
method: GET
auth:
  type: bearer
  credentials:
    token: ZJeXbA5JNZ-AIppandfzGDNRqSeEre2TGNr3nuaGGtA
order: 5000
